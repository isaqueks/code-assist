{
  "name": "read_file",
  "description": "This function will read a file given the path. ./ is the project base directory!",
  "strict": true,
  "parameters": {
    "type": "object",
    "required": [
      "file_path"
    ],
    "properties": {
      "file_path": {
        "type": "string",
        "description": "The path to the file that needs to be read"
      }
    },
    "additionalProperties": false
  }
}